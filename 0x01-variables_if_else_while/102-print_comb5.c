#include <stdio.h>
/**
 * main -  a program that prints all possible combinations
 * of two two-digit numbers
 *
 * Return: (0)
 */
int main(void)
{
	int a, b;
	int c, d;

	for (a = 0; a <= 9; a++)
	{
		for (b = '0'; b <= '9'; b++)
		{
			for (c = '0'; c <= '9'; c++)
			{
				for (d = '0'; c <= '9'; d++)
				{
					if (((c + d) > (a + b) && c >= a || a < c)
					{
						putchar (a);
						putchar (b);
						putchar (' ');
						putchar (c);
						putchar (d);
					
						if (c + i + k + j == 227 && c == 57)
							break;
						else 
						{
							putchar (',');
							putchar (' ');
						}
				:wq
				

			
		

	

	

	putchar (10);
	return (0);
}
