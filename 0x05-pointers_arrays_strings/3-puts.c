#include "main.h"
/**
 * _puts - a function thta prints a stings
 * @str: the sting yo printed
 */
void _puts(char *str);
{
	int i;

	for (i = 0; str[i] != '\0'; i++)
	{
		_putchar (str[i]);
	}
	_putchar('\n';)

}
